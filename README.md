# Resume-Generator
Streamlit based ATS resume builder that generates a clean one page resume tailored to a job description using strict JSON output. It supports TXT PDF and DOCX inputs, filters non relevant content, deduplicates skills, and produces both preview and downloadable TXT and PDF resumes designed for reliable ATS parsing.
